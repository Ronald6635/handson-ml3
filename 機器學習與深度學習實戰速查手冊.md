# 《Hands-on ML, 3rd Edition》程式範例速查表格 (依主題分類)

## Prompts
- 以目錄下的所有.ipynb檔為參考來源

- 將表格內容依據機器學習的**主題領域**進行劃分，可以大幅提高資訊的組織性和可讀性。

- 原來源涵蓋了基礎的數據準備、線性模型、SVMs、決策樹與集成方法，以及深度學習核心組件。
    - 進一步需要涵蓋《Hands-on ML, 3rd Edition》的所有 19 個章節（包括深度學習的進階應用、NLP、生成式模型和強化學習），在原有的基礎上增加多個分類來容納這些進階主題。

以下是依據主題分類整理後的《Hands-on ML, 3rd Edition》程式範例速查表格：

### 《Hands-on ML, 3rd Edition》程式範例速查表格 (依主題分類)

| 主題分類 (Thematic Category) | 模組/類別 (Class/Function) | 程式庫 (Library) | 功能簡介與應用場景 | Fine-tune 用參數 |
| :--- | :--- | :--- | :--- | :--- |
| **數據準備與評估工具** | `GridSearchCV`，`RandomizedSearchCV` | Scikit-Learn (sklearn.model_selection) | **超參數調優**：使用交叉驗證自動搜索最佳超參數組合。應用於尋找最佳模型參數。 | `param_grid` (參數網格)，`n_iter` (隨機搜索的迭代次數)，`cv` (交叉驗證折疊數)。 |
| **(Data Prep & Evaluation)** | `Pipeline`，`ColumnTransformer`，`make_pipeline` | Scikit-Learn (sklearn.pipeline) | 將多個數據轉換步驟和模型**串聯成一個序列**，實現自動化流程。用於處理混合數據類型 (數值和類別)。 | 選擇包含的**轉換器**（Transformers）和最終**模型的所有超參數**。 |
| | `StandardScaler`，`MinMaxScaler`，`SimpleImputer`，`OneHotEncoder` | Scikit-Learn (sklearn.preprocessing) | **數據預處理**：標準化/縮放特徵，處理缺失值（`SimpleImputer` 使用 `median` 或 `most_frequent` 策略），將類別特徵轉換為數值（**獨熱編碼**）。 | `strategy` (Imputer，如 "median")；`handle_unknown` (OneHotEncoder，如 "ignore")；`feature_range` (MinMaxScaler)。 |
| | `train_test_split` | Scikit-Learn (sklearn.model_selection) | 將數據集分割為訓練集和測試集。 | `test_size`, `train_size`, `random_state`, `shuffle`, `stratify`。 |
| | `StratifiedShuffleSplit` | Scikit-Learn (sklearn.model_selection) | 分層隨機分割，確保每個折疊中的類別分佈與原始數據相同。 | `n_splits`, `test_size`, `train_size`, `random_state`。 |
| | `StratifiedKFold` | Scikit-Learn (sklearn.model_selection) | 分層 K 折交叉驗證。 | `n_splits`, `shuffle`, `random_state`。 |
| | `cross_val_predict`, `cross_val_score` | Scikit-Learn (sklearn.model_selection) | 交叉驗證預測和評分。 | `estimator`, `X`, `y`, `cv`, `scoring`。 |
| | `confusion_matrix`, `precision_score`, `recall_score`, `roc_auc_score`, `roc_curve` | Scikit-Learn (sklearn.metrics) | 分類指標計算。 | `y_true`, `y_pred`, `average` 等。 |
| | `learning_curve` | Scikit-Learn (sklearn.model_selection) | 繪製學習曲線，顯示訓練和驗證分數隨樣本數量的變化。 | `estimator`, `X`, `y`, `train_sizes`, `cv`。 |
| | `IsolationForest` | Scikit-Learn (sklearn.ensemble) | 異常檢測演算法。 | `n_estimators`, `contamination`, `random_state`。 |
| | `OrdinalEncoder` | Scikit-Learn (sklearn.preprocessing) | 將類別特徵編碼為整數。 | `categories`, `dtype`。 |
| | `FunctionTransformer` | Scikit-Learn (sklearn.preprocessing) | 使用任意可調用函數構造轉換器。 | `func`, `inverse_func`, `validate`。 |
| | `BaseEstimator`, `TransformerMixin` | Scikit-Learn (sklearn.base) | 基類，用於繼承自定義估計器。 | N/A。 |
| | `check_array`, `check_is_fitted` | Scikit-Learn (sklearn.utils.validation) | 輸入驗證工具。 | 各種參數。 |
| | `SGDClassifier`，`f1_score()`，`ROC curve` | Scikit-Learn (sklearn.linear_model, sklearn.metrics) | **分類評估**：隨機梯度下降分類器適用於大規模數據。性能評估使用 **F1 分數** (Precision/Recall 權衡) 和 **ROC 曲線**。 | 調整**決策閾值** (Threshold)；`loss` (e.g., **"hinge"**)。 |
| **線性與廣義線性模型** | `LinearRegression` | Scikit-Learn (sklearn.linear_model) | **線性迴歸**：使用 Normal Equation 或 SVD 直接計算最佳模型參數（最小化 MSE）。 | 無（封閉式解法）。 |
| **(Linear & Generalized Linear Models)** | `Ridge`，`Lasso`，`ElasticNet` | Scikit-Learn (sklearn.linear_model) | **正規化線性模型**：分別使用 $\ell_2$、$\ell_1$ 或兩者組合來約束權重，以減少過度擬合。 | **正規化強度 $\alpha$** (alpha)：值越大，約束越強。 |
| | `LogisticRegression` | Scikit-Learn (sklearn.linear_model) | **羅吉斯迴歸**：用於分類任務，估計類別機率。 | **$C$** (正規化強度的倒數)；`penalty` (e.g., 'l2')。 |
| | `SGDRegressor` | Scikit-Learn (sklearn.linear_model) | 線性模型，使用 SGD 訓練。 | `loss`, `penalty`, `alpha`, `learning_rate`, `eta0`。 |
| | `TransformedTargetRegressor` | Scikit-Learn (sklearn.compose) | 對目標變數進行轉換的元估計器。 | `regressor`, `transformer`。 |
| | `add_dummy_feature` | Scikit-Learn (sklearn.preprocessing) | 向數據集添加截距列。 | `X`, `value`。 |
| | `PolynomialFeatures` | Scikit-Learn (sklearn.preprocessing) | 生成多項式和交互特徵。 | `degree`, `interaction_only`, `include_bias`。 |
| | `mean_squared_error`, `root_mean_squared_error` | Scikit-Learn (sklearn.metrics) | 均方誤差和均方根誤差。 | `y_true`, `y_pred`。 |
| **支持向量機** | `SVC`，`LinearSVC`，`SVR` | Scikit-Learn (sklearn.svm) | **SVM 分類與迴歸**：**大邊界分類** (Large Margin Classification)。使用 **核心技巧** (Kernel Trick) 處理非線性數據。 | **$C$** (正規化參數)；**`kernel`** 類型 (e.g., **"rbf"**, "poly")；**$\gamma$** (Gamma，RBF 核心參數)。 |
| **(Support Vector Machines, SVMs)** | `hinge loss function` | 數學/理論 | **SVM 理論基礎**：硬邊界與軟邊界分類。對偶問題。 | N/A；相關參數為 $C$ 和 $\gamma$。 |
| | `LinearSVR` | Scikit-Learn (sklearn.svm) | 線性支持向量迴歸。 | `epsilon`, `C`, `loss`。 |
| | `rbf_kernel` | Scikit-Learn (sklearn.metrics.pairwise) | RBF 核心函數。 | `X`, `Y`, `gamma`。 |
| **決策樹與集成方法** | `DecisionTreeClassifier`/`Regressor` | Scikit-Learn (sklearn.tree) | **決策樹**：**白箱模型**。利用 **CART 演算法** 和 **Gini 不純度/熵** 進行分割。 | **`max_depth`** (最大深度)；**`min_samples_leaf`** (葉節點最小樣本數)；用於**正則化**。 |
| **(Decision Trees & Ensembles)** | `RandomForestClassifier`，`BaggingClassifier`，`VotingClassifier` | Scikit-Learn (sklearn.ensemble) | **隨機森林**：通過 **Bagging** 結合多棵決策樹，有效**降低變異性**。`VotingClassifier` 結合不同模型的預測結果。 | **`n_estimators`** (樹的數量)；**`max_features`** (每次分割時隨機採樣的特徵數)；**`voting`** 類型 ('hard' 或 'soft')。 |
| | `GradientBoostingRegressor`/`Classifier` | Scikit-Learn (sklearn.ensemble) | **梯度提升** (Boosting)：序列訓練，每一步都糾正前一個模型的**殘差誤差**，以**降低偏差**。 | **`learning_rate`** (學習率，用於收縮), **`n_estimators`** (樹的數量), `max_depth`。 |
| | `AdaBoostClassifier` | Scikit-Learn (sklearn.ensemble) | AdaBoost 分類器。 | `n_estimators`, `learning_rate`, `algorithm`。 |
| | `ExtraTreesClassifier` | Scikit-Learn (sklearn.ensemble) | 極端隨機樹分類器。 | `n_estimators`, `criterion`, `max_depth`。 |
| | `HistGradientBoostingRegressor` | Scikit-Learn (sklearn.ensemble) | 基於直方圖的梯度提升迴歸樹。 | `learning_rate`, `max_iter`, `max_leaf_nodes`。 |
| | `StackingClassifier` | Scikit-Learn (sklearn.ensemble) | 堆疊估計器。 | `estimators`, `final_estimator`, `cv`。 |
| | `MLPClassifier` | Scikit-Learn (sklearn.neural_network) | 多層感知器分類器。 | `hidden_layer_sizes`, `activation`, `solver`。 |
| | `export_graphviz` | Scikit-Learn (sklearn.tree) | 將決策樹導出為 GraphViz DOT 格式文件，用於可視化。 | `out_file`, `feature_names`, `class_names`, `rounded`, `filled`。 |
| | `Source.from_file` | Graphviz (graphviz) | 從 DOT 文件讀取並顯示決策樹圖形，用於可視化 `export_graphviz` 導出的樹結構。 | `filepath` (DOT 文件路徑)；無其他參數。 |
| **降維與流形學習** | `PCA`，`RandomizedPCA`，`KernelPCA` | Scikit-Learn (sklearn.decomposition) | **主成分分析** (PCA)：**最流行降維演算法**。通過 **SVD** 找出保留**最大變異數**的超平面。 | **`n_components`** (目標維度或解釋變異數比例，如 95%)。`svd_solver="randomized"` (用於加速)。 |
| **(Dimensionality Reduction)** | `LocallyLinearEmbedding` (LLE) | Scikit-Learn (sklearn.manifold) | **流形學習**：用於處理**非線性數據集**（如 Swiss Roll）。 | `n_neighbors` (k-nearest neighbors)；`max_iter`。 |
| | `IncrementalPCA` | Scikit-Learn (sklearn.decomposition) | 增量主成分分析。 | `n_components`, `whiten`。 |
| | `LinearDiscriminantAnalysis` | Scikit-Learn (sklearn.discriminant_analysis) | 線性判別分析。 | `solver`, `shrinkage`, `n_components`。 |
| | `Isomap` | Scikit-Learn (sklearn.manifold) | Isomap 嵌入。 | `n_neighbors`, `n_components`。 |
| | `MDS` | Scikit-Learn (sklearn.manifold) | 多維縮放。 | `n_components`, `metric`。 |
| | `TSNE` | Scikit-Learn (sklearn.manifold) | t-分佈隨機鄰域嵌入。 | `n_components`, `perplexity`, `learning_rate`。 |
| | `GaussianRandomProjection`, `SparseRandomProjection` | Scikit-Learn (sklearn.random_projection) | 隨機投影。 | `n_components`, `eps`。 |
| | `johnson_lindenstrauss_min_dim` | Scikit-Learn (sklearn.random_projection) | 找到安全的隨機投影維度。 | `n_samples`, `eps`。 |
| **非監督式學習** | `KMeans`，`DBSCAN`，`GaussianMixture` (GMM) | Scikit-Learn (sklearn.cluster, sklearn.mixture) | **聚類分析** (Clustering)：用於**客戶分群**、**異常偵測**。DBSCAN 基於密度，GMM 基於概率分佈。 | `n_clusters` (KMeans, GMM)；`eps` 和 `min_samples` (DBSCAN)。 |
| **(Unsupervised Learning)** | `Autoencoder` | Keras/TensorFlow | **自動編碼器**：用於降維、特徵提取、**生成式模型** (Generative Models)。 | **`latent_dim`** (編碼層維度) ；**權重綁定** (Tying Weights)；`reconstruction_loss`。 |
| | `AgglomerativeClustering` | Scikit-Learn (sklearn.cluster) | 凝聚聚類。 | `n_clusters`, `affinity`, `linkage`。 |
| | `MiniBatchKMeans` | Scikit-Learn (sklearn.cluster) | 小批量 K-Means 聚類。 | `n_clusters`, `batch_size`。 |
| | `SpectralClustering` | Scikit-Learn (sklearn.cluster) | 譜聚類。 | `n_clusters`, `affinity`。 |
| | `silhouette_samples`, `silhouette_score` | Scikit-Learn (sklearn.metrics) | 輪廓係數計算。 | `X`, `labels`, `metric`。 |
| | `BayesianGaussianMixture` | Scikit-Learn (sklearn.mixture) | 變分貝葉斯高斯混合。 | `n_components`, `covariance_type`。 |
| **深度學習核心組件** | `tf.keras.models.Sequential`，`tf.keras.layers.Dense` | Keras/TensorFlow | **序列模型** (Sequential API)：構建 MLP 網路。**全連接層**。 | `units` (神經元數量)，`activation` (激活函數)，`kernel_initializer`。 |
| **(Deep Learning Core Components)** | `tf.keras.layers.BatchNormalization` | Keras/TensorFlow | **批次正規化** (BN)：**加速訓練**並緩解**梯度不穩定性**。 | `momentum` (用於移動平均)；`axis` (決定正規化的維度)。 |
| | `tf.keras.layers.Dropout`，`tf.keras.regularizers.l1`/`l2` | Keras/TensorFlow | **正則化技術**：**Dropout** 減少神經元共適應，提升泛化能力。L1/L2 懲罰項約束權重。 | **`rate`** (丟棄的機率，例如 0.5)；`factor` (正規化強度)。 |
| | `Perceptron`, `MLPClassifier`, `MLPRegressor` | Scikit-Learn (sklearn.linear_model, sklearn.neural_network) | 感知器和多層感知器。 | `hidden_layer_sizes`, `activation`, `solver`。 |
| **TensorFlow 底層與客製化** | `tf.GradientTape`，`@tf.function` | TensorFlow | **自動微分** (Autodiff)：記錄計算過程以**計算梯度**。使用 `@tf.function` 將 Python 函數轉換為 **TensorFlow Graph** 進行優化。 | 無 (用於控制梯度計算的上下文)。 |
| **(Low-Level TF & Customization)** | `tf.Variable`，`tf.constant`，`tf.placeholder` | TensorFlow | **張量與操作**：`Variable` (可變參數)；`constant` (常量)；`placeholder` (外部輸入)。 | N/A。 |
| **數據管道與特徵預處理** | `tf.data.Dataset`，`tf.data.TextLineDataset`，`interleave()` | TensorFlow (`tf.data`) | **高效數據加載**：處理大規模數據 (如 CSV, TFRecords)。使用 `interleave()` 進行**並行讀取**以提高效率。 | `cycle_length` (並行讀取的文件數)；`batch_size`。 |
| **(Data Pipelines & Feature Prep)** | `tf.keras.layers.Embedding`，`TextVectorization`，`CategoryEncoding` | Keras/TensorFlow | **特徵預處理層**：將文本/類別特徵轉換為數值向量。**詞嵌入** (Word Embeddings) 處理高維文本。 | **`output_dim`** (Embedding 嵌入維度)；`output_mode` ("tf_idf", "multi_hot", "count")。 |
| | `tensorflow.train` | TensorFlow | 用於創建 TFRecord 文件的工具。 | N/A。 |
| | `tensorflow_datasets` | TensorFlow | 數據集集合，可與 TensorFlow 一起使用。 | N/A。 |
| | `tensorflow_hub` | TensorFlow | 訓練過的機器學習模型倉庫。 | N/A。 |
| **深度電腦視覺** | `Conv2D`, `MaxPooling2D`，`Conv2DTranspose` | Keras/TensorFlow | **卷積神經網路** (CNN) 基礎：**卷積層**提取局部特徵，**池化層**進行降採樣。 | **`filters`** (濾鏡數量), **`kernel_size`** (核心尺寸), **`strides`** (步幅)；`padding` ("VALID" 或 "SAME")。 |
| **(Deep Computer Vision)** | `ResNet`, `YOLO` (架構) | Keras/TensorFlow | **進階 CNN 架構**：**ResNet** 使用 **Skip Connections** 解決深層網路梯度問題。**YOLO** (You Only Look Once) 應用於**物體偵測** (Object Detection)。 | **遷移學習**中的**凍結層** (`Trainable=False`)。 |
| | `tensorflow_datasets` | TensorFlow | 數據集集合。 | N/A。 |
| **序列處理與 RNNs/Attention** | `LSTM`，`GRU`，`SimpleRNN` | Keras/TensorFlow | **循環神經網路** (RNNs)：用於**時間序列預測**。**LSTM** 和 **GRU** 解決梯度消失/長期依賴問題。 | **`units`** (隱藏單元數量)；`return_sequences`；`stateful` (有狀態 RNN)。 |
| **(Sequences, RNNs & Attention)** | `MultiHeadAttention`，`Transformer` (架構)，`Positional Encoding` | Keras/TensorFlow | **注意力機制** (Attention)：用於 **NMT** (機器翻譯)。**Transformer** 架構完全基於注意力，通過**位置編碼** (Positional Encoding) 引入序列順序。 | **`num_heads`** (Attention 頭數)；Beam Search **`beam width`** (用於推論)。 |
| | `statsmodels` | StatsModels | 統計建模和計量經濟學庫。 | N/A。 |
| **生成式模型** | `Autoencoder`，`GAN` (Generator/Discriminator)，`DDPM` (Diffusion Models) | Keras/TensorFlow | **自動編碼器** (Autoencoders)：用於數據壓縮。**VAE** (變分自動編碼器)；**生成式對抗網路** (GANs)；**擴散模型** (DDPM) 用於高品質圖像生成。 | **`latent_dim`** (潛在空間維度)；**損失函數** (e.g., VAE 的 KL 散度項)；訓練**生成器和判別器的平衡** (GANs)。 |
| **強化學習** | `gym.make()`, `Policy Gradients`, `DQN` (Deep Q-Network) | OpenAI Gym, Keras/TF | **環境互動** (如 `CartPole`)。**策略梯度** (Policy Gradients, PG)；**深度 Q 網路** (DQN) 及其變體。 | **`discount_factor` ($\gamma$)**；**探索策略** ($\epsilon$, for $\epsilon$-greedy)；`learning_rate`。 |
| **(Reinforcement Learning, RL)** | `env.step()`, `env.reset()` | OpenAI Gym | **環境操作**：`step()` 執行動作並返回新的觀測、獎勵和狀態；`reset()` 進行環境初始化。 | N/A。 |
| | `gymnasium` | Gymnasium | 開發和比較強化學習演算法的工具包。 | N/A。 |
| **規模化與部署** | `tf.distribute.MirroredStrategy`，`MultiWorkerMirroredStrategy` | TensorFlow Distributed API | **分散式訓練**：在多 GPU 或多台機器上進行**數據并行化** (Data Parallelism)，以加速大規模模型的訓練。 | **`batch_size`** (需能被副本數整除)。 |
| **(Scaling & Deployment)** | `TFLiteConverter`，`tf.saved_model.save`，`Keras Tuner` | TFLite, Keras/TF | **部署與優化**：將模型轉換為 TFLite 格式以部署到行動/嵌入式設備；使用 **Keras Tuner** 進行超參數自動優化。 | 超參數調優的**搜索範圍** (Search Space)；`max_trial_count` (最大嘗試次數)。 |
| | `google.cloud.aiplatform` | Google Cloud | Vertex AI 用於機器學習。 | N/A。 |
| | `hypertune` | Hypertune | 超參數調優庫。 | N/A。 |
| | `keras_tuner` | Keras Tuner | Keras 模型的超參數調優。 | N/A。 |
| | `tensorboard` | TensorBoard | TensorFlow 可視化工具包。 | N/A。 |
| | `tqdm` | tqdm | 進度條庫。 | N/A。 |
| | `transformers` | Transformers | 最新的自然語言處理庫。 | N/A。 |
| | `PIL` | Pillow | Python 圖像處理庫。 | N/A。 |
| | `nltk` | NLTK | 自然語言處理工具包。 | N/A。 |
| | `graphviz` | Graphviz | 圖形可視化軟體。 | N/A。 |
